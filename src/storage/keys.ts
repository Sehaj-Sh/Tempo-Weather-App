/** Central AsyncStorage keys for Tempo app preferences and data. */
export const STORAGE_KEYS = {
  appearance: '@tempo/appearance',
  preferences: '@tempo/preferences',
  savedCities: '@tempo/savedCities',
} as const;
